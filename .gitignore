
# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
htmlcov/
tmp-*.log
.kanban-snapshots/
.adk/*
!.adk/release-state.db
logs/
# Published maintenance evidence logs (Docusaurus links under docs/maintenance/logs/)
!docs/maintenance/logs/
!docs/maintenance/logs/**

# macOS
.DS_Store

# VS Code
.vscode/

# Local environment configuration (untracked)
.env
.env.local

# ECC harness bridge (copied from template per project; not committed)
ecc-adk-bridge.yaml

# Windsurf (migrated to .cursor/skills/)
.windsurf/
.coverage
SCRATCHPAD.md

# FR-044 — RW temporary execution log (local forensic artifact)
.rw-execution.log
# RW Step 7 stamp/snapshot scratch (canonical: changelog-archive/four-surface-reports/)
.rw-step7-snapshot/
.rw-step7-stamp-evidence.json
.rw-step7-evidence-*.json
docs/journals/agent-runs/

# FR-113 / E08:S03:T17 — Local Code Quality Gate artifacts (local only)
.cqg/

# FR-115 / FR-116 — Release metadata read model (regenerable via ingest)
.data/

# RW Journal Artifacts — runtime per-run files, not operator documentation
# Operator docs (README.md, JOURNAL_SCHEMA.md, RECOVERY_PROCEDURE.md, ROLLBACK_CONTRACT.md)
# are kept in version control; runtime rw-* artifacts are not.
docs/journals/rw-*.json
docs/journals/rw-*-recovery.json
docs/journals/rw-*-recovery.md
