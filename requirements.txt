# Core dependencies for ai-dev-kit CLI tool
pyyaml>=6.0.3
Babel>=2.12

# Documentation and validation dependencies (Python / PyPI only)
# markdownlint-cli2 is npm-only — install via: npm install -g markdownlint-cli2
click>=8.4.1
requests>=2.34.2

# Development dependencies (install with: pip install -e ".[dev]")
# pytest>=7.0
# pytest-cov>=4.0
# black>=23.0
# flake8>=6.0
# mypy>=1.0

